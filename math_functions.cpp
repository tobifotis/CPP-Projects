// Useful math related functions (#include cmath)

#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main () {
	double x = 45.9;
	double y = 45.7;
	double z;


	z = abs (-512);
	// z = pow (5, 3);
	// z = sqrt (81);

	// z = min (x, y);
	// z = max (x, y);

cout << z;








	return 0;
}